spring:
  main:
    banner-mode: "off"
  datasource:
    url: jdbc:tc:postgres:14-alpine3.16:///sn_storage_service_test?TC_INITSCRIPT=db/init_test.sql
    #driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
    username: sn_user_test
    password: sn_user_test
  jpa:
    #database-platform: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: none

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        cache: DEBUG
        stat: DEBUG
